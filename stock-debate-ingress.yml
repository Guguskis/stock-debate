apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stock-debate-ingress
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: authentication-service
                port:
                  number: 8081